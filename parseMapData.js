var parseMapData = function (settings) {
    settings.MapLength = 0;
    settings.BackgroundSpriteData = []; //sprite data, array of arrays. load one 0 for every time at this point, with each arary of tiles as a parallax
    settings.CollisionData = []; //array of arrays. 0 is movable tile, 1 is blocking
    
    //logic
    
    return settings
}